name = "build-server-lb"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

[vars]
# Non-secret vars (overridden per env below). Secrets set via: wrangler secret put <KEY>
# WEBHOOK_SECRET — set via: wrangler secret put WEBHOOK_SECRET
# SERVER1_URL    — set via: wrangler secret put SERVER1_URL
# SERVER2_URL    — set via: wrangler secret put SERVER2_URL
